import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.reddah.atlas',
  appName: 'atlas',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
